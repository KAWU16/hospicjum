---
label: blog_wpis
hide_body: true
fields:
- name: image
  type: file
  config:
    maxSize: 64
  label: image
- name: opis
  type: textarea
  default: ''
  config:
    required: false
    wysiwyg: false
    schema:
      format: markdown
  label: opis
